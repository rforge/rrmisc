```{r, echo=FALSE, eval=TRUE, fig.align="center", out.width="90%", fig.cap=SubFigCap}
# ```{r echo = FALSE, result = "asis"}
# `r SubTitle`
  filens <- NULL
  j <- 1
  for (j in 1:get(SubName))
  {
    CurNparts <- ifelse(j < 10, paste0("0", j), j)
    filens    <- get(paste0(SubFigName, CurNparts))
    # filens    <- c(filens, file.path(tempDirFigures, paste0(SubFigName, CurNparts, ".png")))
    # file.exists(filens)
  }
  knitr::include_graphics(path = filens)    # Vektor als Attribut für include_graphics()
```

